<?php session_start(); 
include "koneksi.php"; 
$username=$_POST['username']; 
$password=$_POST['password']; 
$query=mysql_query("select * from admin where username='$username' and password='$password'"); 
$cek=mysql_num_rows($query); 
if($cek){ 
$_SESSION['username']=$username; 
?>
<style type="text/css">
body p {
	text-align: center;
}
</style>

<p>Anda berhasil login. silahkan menuju</p>
<p> <a href="home.php">Halaman HOME</a>
  <?php 
}else{ 
?>Anda gagal login. silahkan</p>
<p> <a href="form_admin.php">Login kembali</a></p>
<?php 
echo mysql_error(); 
} 
?> 
